const placeTaba = 6;
const placeSharm = 15;
const placeHurghada = 25;

const message = prompt("Введіть кількість участників групи");
const number = parseInt(message);

function isInteger() {
  return (num ^ 0) === num;
}
